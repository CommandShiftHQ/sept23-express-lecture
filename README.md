## Express Lecture Repo
Fork, then clone using

`git clone https://github.com/CommandShiftHQ/sept23-express-lecture.git`

Change into the directory `cd sept23-express-lecture`

To install `npm install`

To run `npm start`

